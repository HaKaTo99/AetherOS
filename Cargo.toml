[workspace]
resolver = "2"
members = [
    "kernel",
    "compiler",
    "booter",
]
default-run = "aetheros-kernel"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1
