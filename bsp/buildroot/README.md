# Buildroot BSP (Example)

This folder contains guidance to create a Buildroot-based BSP and image for small devices.

- Create a `board/aetheros/<machine>/` with defconfig and post-build scripts.
- Add kernel recipe and device tree blobs.
- Integrate AetherOS userland and Q-HAL packages.
