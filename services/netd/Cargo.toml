[package]
name = "netd"
version = "0.1.0"
edition = "2021"

[dependencies]
libaether = { path = "../../apps/libaether" }
smoltcp = { version = "0.10", default-features = false, features = ["proto-ipv4", "socket-tcp", "socket-udp", "async"] }
